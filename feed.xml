<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://pengkun-jiao.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://pengkun-jiao.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-05T03:41:38+00:00</updated><id>https://pengkun-jiao.github.io/feed.xml</id><title type="html">Pengkun Jiao「焦鹏昆」</title><subtitle>Pengkun Jiao&apos;s website. </subtitle><entry><title type="html">OPD 随想</title><link href="https://pengkun-jiao.github.io/blog/2026/opd/" rel="alternate" type="text/html" title="OPD 随想"/><published>2026-08-01T01:00:00+00:00</published><updated>2026-08-01T01:00:00+00:00</updated><id>https://pengkun-jiao.github.io/blog/2026/opd</id><content type="html" xml:base="https://pengkun-jiao.github.io/blog/2026/opd/"><![CDATA[<h2 id="引言">引言</h2> <p>笔者最近在做 MOPD 研究时发现，在计算 Advantage（教师-学生预测概率的 log-ratio）时，仅用学生采样的 top-1 token 计算已经效果很好了。巧合的是，Kimi K3 技术报告中也称，top-k 采样的消融未见明显性能或效率提升。因此有感而发，在本文中讨论一个问题：在 On-Policy Distillation 中，为什么不应该多步在全词表上计算 Advantage？</p> <hr/> <h2 id="背景on-policy-distillation-的目标">背景：On-Policy Distillation 的目标</h2> <p>在线蒸馏的目标是让 student \(q_\theta\) 逼近 teacher \(p\)，并且是在 student 的采样轨迹上，以防止暴露偏差（exposure bias）或分布不匹配。从信息论角度，最自然的目标是最小化 reverse KL 散度：</p> \[\mathcal{L}(\theta) = \mathbb{E}_{x \sim p_x}\left[\mathrm{KL}\!\left[q_\theta(\cdot|x) \,\|\, p(\cdot|x)\right]\right]\] <p>由于序列空间的维度灾难，直接计算该期望不可行，需通过采样估计梯度。对序列生成任务展开：</p> \[\mathcal{L}(\theta) = \mathbb{E}_{x \sim p_x,\; y \sim q_\theta}\left[\sum_{t=1}^{T} \log \frac{q_\theta(y_t|y_{&lt;t}, x)}{p(y_t|y_{&lt;t}, x)}\right]\] <table> <tbody> <tr> <td>对 \(\theta\) 求梯度时，注意到损失函数中有<strong>两处</strong> \(\theta\) 的依赖：采样分布 \(y \sim q_\theta\)，以及被期望的函数 $$f(y;\theta) = \sum_t \log\frac{q_\theta(y_t</td> <td>y_{&lt;t},x)}{p(y_t</td> <td>y_{&lt;t},x)}\(本身也含\)\theta$$。因此梯度由两项构成：</td> </tr> </tbody> </table> \[\nabla_\theta \mathcal{L}(\theta) = \underbrace{\mathbb{E}_{y\sim q_\theta}\!\left[f(y;\theta)\,\nabla_\theta\log q_\theta(y)\right]}_{\text{log-trick 项}} + \underbrace{\mathbb{E}_{y\sim q_\theta}\!\left[\nabla_\theta f(y;\theta)\right]}_{\text{直接梯度项}}\] <p>其中直接梯度项为：</p> \[\mathbb{E}_{y\sim q_\theta}\!\left[\nabla_\theta f(y;\theta)\right] = \mathbb{E}_{y\sim q_\theta}\!\left[\sum_{t=1}^T \nabla_\theta \log q_\theta(y_t|y_{&lt;t},x)\right]\] <p>合并两项，得到完整的 Policy Gradient 形式梯度：</p> \[\nabla_\theta \mathcal{L}(\theta) = \mathbb{E}_{y\sim q_\theta}\!\left[\sum_{t=1}^T (R_t - 1)\cdot \nabla_\theta \log q_\theta(y_t|y_{&lt;t},x)\right]\] <p>其中即时奖励与多步 Return 分别定义为：</p> \[r_t = \log \frac{q_\theta(y_t|y_{&lt;t}, x)}{p(y_t|y_{&lt;t}, x)}, \qquad R_t = \sum_{t'=t}^{T} r_{t'}\] <p>这是标准的 REINFORCE 结构，\(R_t\) 为从第 \(t\) 步起的累积奖励，\(-1\) 项来自 \(f\) 对 \(\theta\) 的直接偏导，等价于一个常数 baseline，不影响梯度方向，但影响方差。</p> <hr/> <h2 id="问题的提出">问题的提出</h2> <p>在实现中，一个看似自然的想法是：</p> <blockquote> <p>既然每个 token 位置 \(t\) 上，\(q_\theta\) 已经计算了整个词表的概率分布，能否不采样单个 \(y_t\)，而是对全词表 \(V\) 枚举，用加权求和替代采样，从而降低方差？</p> </blockquote> <p>形式上，这将梯度估计从<strong>单点采样</strong>：</p> \[\hat{g}_t^{\text{sample}} = R_t \cdot \nabla_\theta \log q_\theta(y_t|y_{&lt;t}, x), \quad y_t \sim q_\theta\] <p>替换为<strong>全词表期望</strong>：</p> \[\hat{g}_t^{\text{vocab}} = \sum_{v \in V} R_t(v) \cdot \nabla_\theta \log q_\theta(v|y_{&lt;t}, x) \cdot q_\theta(v|y_{&lt;t}, x)\] <p>这个替换看起来用期望取代了采样，方差更低，实则<strong>从根本上破坏了轨迹一致性</strong>。</p> <hr/> <h2 id="核心问题轨迹一致性">核心问题：轨迹一致性</h2> <h3 id="什么是合法的轨迹">什么是合法的轨迹？</h3> <p>Policy Gradient 的理论基础要求梯度估计量作用在从 \(q_\theta\) <strong>真实采样</strong>的完整轨迹上。一条合法轨迹为：</p> \[y = (y_1, y_2, \ldots, y_T), \quad y_t \sim q_\theta(\cdot|y_{&lt;t}, x)\] <p>每个 \(y_t\) 与后续 \(y_{t+1}, \ldots, y_T\) 之间存在真实的<strong>因果依赖关系</strong>：\(y_{t+1}\) 是在已知 \(y_t\) 的条件下采样的。</p> <h3 id="多步-return-的语义">多步 Return 的语义</h3> <p>\(R_t\) 的含义是：<strong>在执行动作 \(y_t\) 之后，沿当前策略继续走完整条轨迹所获得的累积奖励</strong>：</p> \[R_t = r_t(y_t) + r_{t+1}(y_{t+1}) + \cdots + r_T(y_T)\] <p>其中 \(y_{t+1}, \ldots, y_T\) 是在 \(y_t\) 确定之后继续从 \(q_\theta\) 采样得到的。因此 \(R_t\) 是 \((y_t, y_{t+1}, \ldots, y_T)\) 的<strong>联合函数</strong>，记作 \(R_t(y_t, y_{t+1}, \ldots, y_T)\)。</p> <h3 id="全词表枚举时发生了什么">全词表枚举时发生了什么？</h3> <p>在全词表枚举中，对每个候选 \(v \in V\)，需要给出 \(R_t(v)\)。但问题在于：\(y_{t+1}, \ldots, y_T\) 从哪里来？实践中只有两种方案，<strong>两种均有致命缺陷</strong>。</p> <p><strong>方案 A：固定后续轨迹（有偏）</strong></p> <p>用同一条采样轨迹的后续部分 \(\hat{y}_{t+1}, \ldots, \hat{y}_T\) 对所有候选 \(v\) 共用：</p> \[R_t(v) \approx r_t(v) + \sum_{t'=t+1}^{T} r_{t'}(\hat{y}_{t'})\] <table> <tbody> <tr> <td><strong>问题</strong>：\(\hat{y}_{t+1}\) 是在 \(\hat{y}_t\) 的条件下采样的，即 $$\hat{y}<em>{t+1} \sim q</em>\theta(\cdot</td> <td>\hat{y}<em>t, y</em>{&lt;t}, x)\(。当候选换成\)v \neq \hat{y}_t\(时，这条后续轨迹对\)v$$ 而言并不合法。</td> </tr> </tbody> </table> <p>数学上，正确的期望分解为：</p> \[\mathbb{E}_{y_t, y_{&gt;t}}[R_t] = \mathbb{E}_{y_t}\!\left[\mathbb{E}_{y_{&gt;t}|y_t}[R_t]\right]\] <p>而选择 A 实际计算的是：</p> \[\sum_v q_\theta(v) \cdot \left[r_t(v) + \underbrace{\sum_{t'&gt;t} r_{t'}(\hat{y}_{t'})}_{\text{与 }v\text{ 无关的常数}}\right]\] <p>后半部分是与 \(v\) 无关的常数，对梯度的贡献退化；同时 \(\hat{y}_{t+1}\) 条件于错误的上文，产生<strong>系统性偏差</strong>。</p> <p><strong>方案 B：对每个 \(v\) 独立 rollout（不可行）</strong></p> <p>对每个 \(v \in V\)，从 \(v\) 出发重新采样 \(y_{t+1}^{(v)}, \ldots, y_T^{(v)}\)，计算各自的 \(R_t^{(v)}\)：</p> \[\hat{g}_t^{\text{vocab}} = \sum_{v \in V} R_t^{(v)} \cdot \nabla_\theta \log q_\theta(v|y_{&lt;t}, x) \cdot q_\theta(v|y_{&lt;t}, x)\] <p>这在数学上无偏，但代价是：</p> <ul> <li> <table> <tbody> <tr> <td>词表大小 $$</td> <td>V</td> <td>\sim 10^5\(，序列长度\)T \sim 10^3$$</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td>每个位置需要 $$</td> <td>V</td> <td>$$ 次完整 rollout</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td>总计算量：$$O(</td> <td>V</td> <td>\cdot T)$$ 次完整前向传播</td> </tr> </tbody> </table> </li> </ul> <p><strong>在任何实际系统中均不可行。</strong></p> <hr/> <h2 id="如何正确地进行全词表对齐">如何正确地进行全词表对齐？</h2> <p>值得特别指出，以下操作<strong>完全合法</strong>：</p> \[\hat{g}_t^{\text{instant}} = \sum_{v \in V} r_t(v) \cdot \nabla_\theta \log q_\theta(v|y_{&lt;t}, x) \cdot q_\theta(v|y_{&lt;t}, x)\] <table> <tbody> <tr> <td>其中 $$r_t(v) = \log \frac{p(v</td> <td>y_{&lt;t}, x)}{q_\theta(v</td> <td>y_{&lt;t}, x)}$$ 仅是<strong>单步即时奖励</strong>，不涉及任何后续轨迹。</td> </tr> </tbody> </table> <p>该量存在精确闭合形式。注意到：</p> \[\sum_{v \in V} \log \frac{p(v|y_{&lt;t},x)}{q_\theta(v|y_{&lt;t},x)} \cdot \nabla_\theta \log q_\theta(v|y_{&lt;t},x) \cdot q_\theta(v|y_{&lt;t},x) = -\nabla_\theta \,\mathrm{KL}\!\left[q_\theta(\cdot|y_{&lt;t},x) \,\|\, p(\cdot|y_{&lt;t},x)\right]\] <p>这正是 <strong>token-level KL 蒸馏（SeqKD）的直接梯度</strong>。</p> <p>它合法的根本原因在于：即时奖励 \(r_t(v)\) 完全由当前位置的概率决定，<strong>不依赖任何后续轨迹</strong>，因此全词表枚举不产生轨迹不一致性问题。</p> <hr/> <h2 id="偏差-方差视角的完整对比">偏差-方差视角的完整对比</h2> <table> <thead> <tr> <th style="text-align: left">方法</th> <th style="text-align: center">偏差</th> <th style="text-align: center">方差</th> <th style="text-align: center">计算量</th> <th style="text-align: left">备注</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">采样 + 即时 reward</td> <td style="text-align: center">无偏</td> <td style="text-align: center">高</td> <td style="text-align: center">\(O(T)\)</td> <td style="text-align: left">高方差，需要 baseline</td> </tr> <tr> <td style="text-align: left">全词表 + 即时 reward</td> <td style="text-align: center">无偏</td> <td style="text-align: center">零</td> <td style="text-align: center">\(O(|V| \cdot T)\)</td> <td style="text-align: left">即 SeqKD，<strong>推荐</strong></td> </tr> <tr> <td style="text-align: left">采样 + 多步 return</td> <td style="text-align: center">无偏</td> <td style="text-align: center">中</td> <td style="text-align: center">\(O(T)\)</td> <td style="text-align: left">标准 PG / REINFORCE</td> </tr> <tr> <td style="text-align: left">全词表 + 固定轨迹 return</td> <td style="text-align: center"><strong>有偏</strong></td> <td style="text-align: center">低</td> <td style="text-align: center">\(O(|V| \cdot T)\)</td> <td style="text-align: left">❌ 错误做法</td> </tr> <tr> <td style="text-align: left">全词表 + 各自 rollout</td> <td style="text-align: center">无偏</td> <td style="text-align: center">低</td> <td style="text-align: center">\(O(|V|^T)\)</td> <td style="text-align: left">理论正确，实践不可行</td> </tr> </tbody> </table> <hr/> <h2 id="正确的梯度结构">正确的梯度结构</h2> <p>综合以上分析，On-Policy Distillation 中合法的梯度估计量有两种形式：</p> <p><strong>形式一：REINFORCE（RL-style 训练）</strong></p> \[\hat{g} = \sum_{t=1}^{T} R_t \cdot \nabla_\theta \log q_\theta(y_t|y_{&lt;t}, x), \quad y \sim q_\theta\] <p>其中 \(y_t\) 是真实采样的 token，\(R_t\) 建立在与 \(y_t\) 具有合法因果关系的后续轨迹之上。</p> <p><strong>形式二：Token-level KL（GKD 范式）</strong></p> \[\hat{g} = \sum_{t=1}^{T} \nabla_\theta \,\mathrm{KL}\!\left[q_\theta(\cdot|y_{&lt;t},x) \,\|\, p(\cdot|y_{&lt;t},x)\right]\] <p>其中 \(y_{&lt;t}\) 可来自 \(q_\theta\) 的 rollout（on-policy）或固定数据集（off-policy）。</p> <hr/> <h2 id="总结">总结</h2> <p><strong>不应该对全词表计算多步 Advantage</strong>，原因可以精确表述为：</p> <blockquote> <p>多步 Return \(R_t\) 是当前 token \(y_t\) 与后续轨迹 \(y_{&gt;t}\) 的<strong>联合函数</strong>。全词表枚举时，若对每个候选 \(v\) 共用同一条后续轨迹，则该后续轨迹与 \(v\) 之间不存在合法的因果关系，导致 return 的计算语义被破坏，产生<strong>有偏的梯度估计</strong>；若为每个 \(v\) 单独 rollout，则计算量根本不可行。</p> </blockquote> <p>这一约束与”改变随机变量的身份”无关——全词表求和在数学上就是期望操作，本身完全合法。<strong>真正的约束来自轨迹一致性</strong>：多步 return 要求 token 与其后续轨迹之间存在真实的条件依赖关系，而</p>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="on-policy distillation"/><category term="OPD"/><category term="full Vocabulary"/><category term="OPD"/><summary type="html"><![CDATA[笔者最近在做 MOPD 研究时发现，在计算 Advantage（教师-学生预测概率的 log-ratio）时，仅用学生采样的 top-1 token 计算已经效果很好了。巧合的是，Kimi K3 技术报告中也称，top-k 采样的消融未见明显性能或效率提升。因此有感而发，在本文中讨论一个问题：在 On-Policy Distillation 中，为什么不应该多步在全词表上计算 Advantage？]]></summary></entry><entry><title type="html">LU-KV: KV Cache Optimization Based on Long-term Utility</title><link href="https://pengkun-jiao.github.io/blog/2026/lu-kv/" rel="alternate" type="text/html" title="LU-KV: KV Cache Optimization Based on Long-term Utility"/><published>2026-02-20T01:00:00+00:00</published><updated>2026-02-20T01:00:00+00:00</updated><id>https://pengkun-jiao.github.io/blog/2026/lu-kv</id><content type="html" xml:base="https://pengkun-jiao.github.io/blog/2026/lu-kv/"><![CDATA[<blockquote> <p>💡 <strong>TL;DR</strong>: When large language models process extremely long texts and run out of memory, how do we cope? LU-KV predicts the “future value of each memory fragment” to decide what to keep and what to discard, enabling models to maintain strong comprehension under tighter memory budgets.</p> </blockquote> <hr/> <h2 id="-background-what-is-kv-cache">📚 Background: What Is KV Cache?</h2> <h3 id="how-do-large-models-read-long-documents">How Do Large Models “Read” Long Documents?</h3> <p>When you ask a large model to analyze a lengthy article, it needs to understand each word while remembering everything it has read before. This “memory” process relies on the <strong>attention mechanism</strong>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Current token ←[attends to]→ All previous tokens
</code></pre></div></div> <p>To accelerate computation, the model caches the “Keys” and “Values” of previous tokens — this is called the <strong>KV Cache</strong>.</p> <h3 id="why-do-we-need-eviction">Why Do We Need Eviction?</h3> <table> <thead> <tr> <th>Scenario</th> <th>Context Length</th> <th>KV Cache Memory Usage</th> </tr> </thead> <tbody> <tr> <td>Casual conversation</td> <td>~1K tokens</td> <td>~0.5 GB</td> </tr> <tr> <td>Reading a paper</td> <td>~32K tokens</td> <td>~16 GB</td> </tr> <tr> <td>Legal contract analysis</td> <td>~128K tokens</td> <td>~64 GB ⚠️</td> </tr> </tbody> </table> <p>When memory runs out, we must <strong>discard part of the cache</strong> — this is KV Cache Eviction. The central question: <strong>What should we discard to minimize performance loss?</strong></p> <hr/> <h3 id="-a-brief-history-of-kv-cache-eviction">📜 A Brief History of KV Cache Eviction</h3> <h4 id="-first-generation-uniform-compression-2023">🔹 First Generation: Uniform Compression (2023)</h4> <p><strong>Core Idea</strong>: Treat all positions equally and discard at a fixed ratio.</p> \[\text{Retention rate} = r \quad \Rightarrow \quad \text{retain } \lfloor r \times N \rfloor \text{ tokens per position}\] <p><strong>Representative Works</strong>: StreamLLM, H2O<br/> <strong>Strengths</strong>: Simple to implement, low computational overhead<br/> <strong>Limitations</strong>: Ignores importance differences across positions and attention heads; critical information may be accidentally discarded</p> <h4 id="-second-generation-structure-prior-driven-2024">🔹 Second Generation: Structure-Prior-Driven (2024)</h4> <p><strong>Core Idea</strong>: Use structural patterns of text and models to guide eviction.</p> \[\text{Retention probability} \propto f(\text{position}, \text{layer depth}, \text{attention pattern})\] <table> <thead> <tr> <th>Method</th> <th>Structural Prior</th> <th>Intuition</th> </tr> </thead> <tbody> <tr> <td><strong>PyramidKV</strong></td> <td>Shallower layers carry more dispersed information</td> <td>Like a pyramid: wide base (keep more), narrow top (keep less)</td> </tr> <tr> <td><strong>HeadKV</strong></td> <td>Different attention heads serve different roles</td> <td>Like a team with division of labor: some focus on details, others on the big picture — budget allocated accordingly</td> </tr> <tr> <td><strong>CAKE</strong></td> <td>Spatially dispersed tokens are more important</td> <td>Like map landmarks: scattered key points are more worth keeping than clustered ordinary ones</td> </tr> </tbody> </table> <p><strong>Progress</strong>: Domain knowledge improves compression efficiency<br/> <strong>Limitations</strong>: Fixed rules, hard to adapt to new tasks or distributional shifts</p> <h4 id="-third-generation-dynamic-adaptation-20252026">🔹 Third Generation: Dynamic Adaptation (2025–2026)</h4> <p><strong>Core Idea</strong>: Dynamically adjust based on real-time statistical signals.</p> \[b_{\ell,h} = g\big(\text{AttentionEntropy}_{\ell,h}, \text{TaskContext}\big)\] <p>where \(b_{\ell,h}\) denotes the retention budget for the \(h\)-th head in layer \(\ell\).</p> <p><strong>Representative Work</strong>: Ada-KV<br/> <strong>Strengths</strong>: Task-adaptive, online adjustment<br/> <strong>Key Limitation</strong>: Still assumes “<strong>high current attention score = high long-term importance</strong>”</p> <blockquote> <p>🎯 <strong>The Core Problem</strong>: This assumption does not always hold! High scores in some attention heads may be merely “transient noise,” while low-scoring heads may capture critical long-range dependencies (e.g., discourse topics, causal chains).</p> </blockquote> <h4 id="-fourth-generation-lu-kv--global-optimization-via-long-term-marginal-utility-2026">🔹 Fourth Generation: LU-KV — Global Optimization via Long-term Marginal Utility (2026)</h4> <p><strong>Core Breakthrough</strong>: Elevates eviction decisions from “instantaneous scoring” to <strong>“long-term value prediction”</strong>.</p> \[\boxed{ \max_{\{b_{\ell,h}\}} \sum_{\ell=1}^{L}\sum_{h=1}^{H} \underbrace{\mathbb{E}\big[\text{Long-horizon Utility} \mid b_{\ell,h}\big]}_{\text{Expected long-term semantic information retention}} \quad \text{s.t.} \quad \sum_{\ell,h} b_{\ell,h} = B_{\text{total}} }\] <p><strong>Intuition</strong>:<br/> Instead of asking “How important is this token <em>right now</em>?”, we ask “<strong>If we keep it, how much will it help future reasoning?</strong>”</p> <hr/> <h3 id="-lu-kv-method-details">🧠 LU-KV: Method Details</h3> <h4 id="step-1-formulate-kv-cache-eviction-as-a-global-combinatorial-optimization-problem">Step 1: Formulate KV Cache Eviction as a Global Combinatorial Optimization Problem</h4> <p>Allocate a fixed memory budget \(B_{\text{total}}\) across \(L \times H\) attention heads, where each head retains \(b_{\ell,h}\) tokens:</p> \[\begin{aligned} \min_{\{b_{\ell,h}\}} \quad &amp; \sum_{\ell=1}^{L}\sum_{h=1}^{H} \mathcal{L}_{\ell,h}(b_{\ell,h}) \\ \text{s.t.} \quad &amp; \sum_{\ell=1}^{L}\sum_{h=1}^{H} b_{\ell,h} = B_{\text{total}} \\ &amp; b_{\ell,h} \in \mathbb{Z}_{\ge 0} \end{aligned}\] <p><strong>Key Challenge</strong>: This is a high-dimensional discrete combinatorial optimization problem — direct solving is computationally intractable.</p> <h4 id="step-2-efficient-solution-via-mathematical-techniques">Step 2: Efficient Solution via Mathematical Techniques</h4> <p>To solve this non-convex optimization problem, LU-KV employs <strong>convex hull relaxation + greedy rounding</strong>:</p> <ol> <li><strong>Convex Hull Relaxation</strong>: Converts the discrete problem into continuous optimization to quickly find a “near-optimal” allocation ratio</li> <li><strong>Greedy Rounding</strong>: Sequentially allocates integer budgets in descending order of marginal utility</li> </ol> <p><strong>Result</strong>: Dramatically faster solving speed with near-optimal solution quality compared to dynamic programming.</p> <h4 id="step-3-offline-learning--zero-online-overhead">Step 3: Offline Learning + Zero Online Overhead</h4> <p>In practice, future decoding information is unavailable during the current decoding step. Fortunately, the information fidelity of each attention head exhibits a degree of stability across tasks. As shown in the figure below, the locally optimal compression rates are similar across different tasks!</p> <div style="text-align:left; clear:both;"> <img src="https://raw.githubusercontent.com/pengkun-jiao/pengkun-jiao.github.io/master/_posts/lu-kv-001.png" width="500"/> </div> <p>Therefore, LU-KV adopts a “learn once, use many times” strategy:</p> <p><strong>🔧 Offline Phase</strong> (run once before deployment):</p> <ul> <li>Analyze the “compression rate vs. utility” curve for each attention head on representative data</li> <li>Build a lookup table \(\Phi\): given target compression rate \(\sigma\), output the optimal retention ratio \(\{r_{\ell,h}\}\) for each head</li> </ul> <p><strong>⚡ Online Phase</strong> (per inference, zero optimization overhead): \(\begin{aligned} 1.\ &amp; \text{Lookup:} \quad \{r_{\ell,h}\} \leftarrow \Phi(\sigma_{\text{target}}) \\ 2.\ &amp; \text{Budgeting:} \quad b_{\ell,h} = \lfloor (1 - r_{\ell,h}) \times T \rfloor \\ 3.\ &amp; \text{Eviction:} \quad \text{Each head independently retains top-}b_{\ell,h}\text{ tokens} \end{aligned}\)</p> <blockquote> <p>✅ <strong>Advantages</strong>:</p> <ul> <li><strong>Metric-agnostic</strong>: Compatible with any instantaneous scoring method (SnapKV, H2O, etc.)</li> <li><strong>Zero online overhead</strong>: Adds only &lt;1ms table lookup time</li> <li><strong>Plug-and-play</strong>: No modification to model architecture or training pipeline required</li> </ul> </blockquote> <hr/> <h3 id="-summary">🎯 Summary</h3> <p>The core value of LU-KV can be summarized as:</p> <table> <thead> <tr> <th>Dimension</th> <th>Key Contribution</th> <th>Practical Impact</th> </tr> </thead> <tbody> <tr> <td>🔬 <strong>Theoretical Innovation</strong></td> <td>First to model KV Cache eviction as a “long-term marginal utility maximization” combinatorial optimization problem</td> <td>Provides a provable theoretical framework for cache management, going beyond heuristic approaches</td> </tr> <tr> <td>⚙️ <strong>Engineering Practicality</strong></td> <td>Offline analysis + online lookup table: zero inference overhead + metric-agnostic design</td> <td>No modification to model or training pipeline; plug-and-play integration with existing systems</td> </tr> </tbody> </table> <blockquote> <p>💡 <strong>One-line Recap</strong>: LU-KV doesn’t “shortsightedly discard low-value cache” — it “retains with greater foresight” — ensuring every byte of a large model’s memory is allocated where it is most likely to generate value.</p> </blockquote> <hr/> <h3 id="-resources">🔗 Resources</h3> <ul> <li>📄 <a href="https://arxiv.org/pdf/2602.08585">Full Paper</a></li> <li>💻 <a href="https://github.com/baidu-baige/LU-KV">Code Repository</a></li> </ul> <hr/> <h3 id="-citation">📄 Citation</h3> <p>If you find this work useful, please cite our paper:</p> <div class="language-bibtex highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">@inproceedings</span><span class="p">{</span>
    <span class="nl">tang2026predicting</span><span class="p">,</span>
    <span class="na">title</span>     <span class="p">=</span> <span class="s">{Predicting Future Utility: Global Combinatorial Optimization 
                 for Task-Agnostic {KV} Cache Eviction}</span><span class="p">,</span>
    <span class="na">author</span>    <span class="p">=</span> <span class="s">{Ziyao Tang and Pengkun Jiao and Xinhang Chen and 
                 Wei Liu and Shiyong Li and Jingjing Chen}</span><span class="p">,</span>
    <span class="na">booktitle</span> <span class="p">=</span> <span class="s">{Forty-third International Conference on Machine Learning}</span><span class="p">,</span>
    <span class="na">year</span>      <span class="p">=</span> <span class="s">{2026}</span><span class="p">,</span>
    <span class="na">url</span>       <span class="p">=</span> <span class="s">{https://openreview.net/forum?id=FQLxcBsKIb}</span>
<span class="p">}</span>
</code></pre></div></div>]]></content><author><name></name></author><category term="research"/><category term="llm"/><category term="efficiency"/><category term="kv-cache"/><category term="attention optimization"/><category term="cache eviction"/><category term="combinatorial optimization"/><category term="ICML 2026"/><summary type="html"><![CDATA[When large language models process extremely long texts and run out of memory, how do we decide what to keep? LU-KV predicts the "long-term marginal utility" of each cached memory fragment to determine what to retain and what to discard, maintaining strong comprehension under tighter memory budgets.]]></summary></entry></feed>